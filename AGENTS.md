# 特殊约定与易错点

- 不把解析库扩展成歌曲管理系统。MajdataPlay 等调用方提供文本并管理歌曲 metadata；文件发现、CLI 和导出留在外围，不成为文本解析的必需依赖。曲绘等媒体只由导出层附带，不进入事件字段或解析必需参数。
- 不添加曲库去重、歌曲注册表、全局 ID、身份 hash 或 UUID。事件只需单次结果内的递增整数 ID；排序后必须同步更新 Slide 头引用。官方同名歌曲按 DX/SD 区分，导出按用户约定的标题、难度编号和类型命名，不自动追加 hash 后缀。
- 不通过物件种类或曲名猜 DX/SD；类型由明确 metadata 或调用方提供。歌曲属性不重复塞进每行事件。
- 已确认的 schema 以用户后续消息和正式文档为准，不反复索要同一确认；HANDOFF 只作历史快照，不追加聊天流水。本文件不复制 README、目录树、字段清单、参数说明、阶段或测试数量。
- parser 只处理语义、时间和诊断；分析、标准化、baseline、难度权重与映射保持独立。正常分析不重新扫描原始 Simai，不输出面向用户的语法树清单。
- 同时同位置的多个声明不能去重；跨小节/窗口的持续物件不能复制计数。Slide 头、分支与连接段按已确认的模型区分，无头 Slide 也必须保留声明时间。
- 分拍不是拍号；注释中的逗号不能推进时间，重复同值 BPM 不能改变物件时间或拍相位。谱面时间原点保留开头休止，音频 offset 只由调用方加一次。
- 以 MajdataPlay 固定 submodule pin 的 MajSimai 源码和播放器行为为主，README 为辅。不能靠日期标签断言兼容；连接 Slide 段时间不能按段数平均分配。
- 保留原文定位，注意 Unicode code point、UTF-8 字节和 C# UTF-16 索引不同。未知语法明确诊断，不静默吞物件或自动猜节奏；维护实际语法支持状态。
- 区分人工 golden、CSV 往返、内部完整性与独立上游对照；没有诊断或 complete=true 不证明与游戏完全一致。批量部分失败必须明确返回非零状态。
- 真实输入和产物放 data/、outputs/；res/ 只保留小型合成样例和配置。不要提交曲库、音频、曲绘、缓存或搬入上游源码；保留用户的 LICENSE、remote 和已有修改。
- 未获要求不主动 commit、push、创建 PR 或运行完整曲库重算；必要验证优先用合成样例和有限真实抽样。
